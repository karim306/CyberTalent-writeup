              

                                          B-flag

                       * First download the bcap file .

                       * Then in the description we can see that  ====>  without flag{} 
                       *So then let's try search about flag but in another form
                       * To searc about somthing you can type on keyboard [ctrl + f ] 
                       *So lets type flag like ======> f14g 
                       * We can see we found result then select the result , then right click on it then choose follow tcp/stream 
                       * finally , take the flag without the flag ======>  analyze_packet_for_fun